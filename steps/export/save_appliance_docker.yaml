- export_docker_rootfs:
  - exec_out: echo "Exporting the docker image to $${rootfs}"
  - exec_out: docker export $(cat MAIN_CONTAINER_ID) > $${rootfs}
  - on_export_clean:
      - exec_out: rm -rf $${rootfs}
