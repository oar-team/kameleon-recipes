- copy_resolv_conf_to_in_context:
  - on_checkpoint: redo
  - exec_out: grep nameserver /etc/resolv.conf > $${rootfs}/etc/resolv.conf
  - on_setup_clean:
    - exec_out: rm $${rootfs}/etc/resolv.conf
