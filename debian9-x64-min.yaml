#==============================================================================
# vim: softtabstop=2 shiftwidth=2 expandtab fenc=utf-8 cc=81 tw=80
#==============================================================================
#
# DESCRIPTION: Debian stretch (9) x64 min Grid'5000 environment
#
#==============================================================================
---
extend: ./debian9-x64-common.yaml

global:
  # Grid'5000 environment variant
  g5k_variant: min
  # Grid'5000 environment version
  g5k_version: 2019020710

bootstrap:
  - "@base"

setup:
  - "@base"
  - clean_unnecessary_packages

export:
  - "@base"
