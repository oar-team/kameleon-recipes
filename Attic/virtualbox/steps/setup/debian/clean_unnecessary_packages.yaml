- clean_unnecessary_packages:
    - on_setup_clean:
        - exec_in: apt-get install -y debfoster
        - exec_in: yes | debfoster --quiet --force -o MaxPriority=standard -oUseRecommends=yes g5k-meta-packages-$${os_release}-$${variant} grub-pc gnupg linux-image-amd64 console-setup rsync locales tgz-g5k firmware-bnx2 firmware-bnx2x puppet-agent puppetlabs-release-pc1 || true
