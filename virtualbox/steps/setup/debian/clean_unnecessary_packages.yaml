- clean_unnecessary_packages:
    - on_setup_clean:
        - exec_in: apt-get install -y debfoster
        - exec_in: yes | debfoster --quiet --force -o MaxPriority=standard -oUseRecommends=yes apt openssh-server grub-pc gnupg linux-image-amd64 console-setup rsync ntp locales debfoster tgz-g5k puppet-agent puppetlabs-release-pc1 || true
        - exec_in: apt-get purge -y debfoster
#        - exec_in: apt-get autoremove -y
